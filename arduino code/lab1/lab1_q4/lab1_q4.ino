//array creation
char array=//whatever number
int gcount = 0;
int bcount = 0;
int rcount = 0;
bool redLED = LOW;
bool greenLED = LOW;
bool blueLED = LOW;
void setup() {
  // put your setup code here, to run once:
for (int i=0,i<length(array),i++);
  if (array[i] == "R") {rcount = rcount+1 && redLED = HIGH};
  if (array[i] == "G") {gcount = gcount+1 && greenLED = HIGH};
  if (array[i] == "B") {bcount = bcount+1 && blueLED = HIGH};
}
void loop() {
  // put your main code here, to run repeatedly:

}
