// initial variable setup/array creation
int arraysize=512;
int numbers[arraysize];
int squares[arraysize];


void setup() {
  // put your setup code here, to run once:
  //filling the numbers array with values
for (int i=0,i<arraysize,i++);
  numbers[i] = i;
}
//filling the squares array with double-squares of numbers;
for (int j=0,i<arraysize,i++);
  squares[j] = 2*j*j;
void loop() {
  // put your main code here, to run repeatedly:

}
